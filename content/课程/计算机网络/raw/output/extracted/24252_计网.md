## 一、选择（10道）

1. 主机甲跟乙建立 TCP 连接，发送 SYN=1，sequence=12001，求乙给甲发的SYN=？ACK=？sequence=？ack=？

2. 分组交换没有的，电路交换有的A．共享带宽B．丢包，乱序C．带宽保障D．每个分组独立寻址

3． DNS

忘了，选的 UDP

4, CSMA/CD

不能用于无线

5. 路由协议的题目A．同一个子网里边都是用一个路由协议B．域间路由协议需要注重网络性能优化C．忘了D．忘了

6. 哪一个不是性能评价的参数A．丢包率B．吞吐量C．延迟E． 网络卡顿

7. 地址划分，问最多有几个 IP 地址，记得减掉两个。

8. 五层模型中哪一层是在用户空间中的？

9. TCP VS UDP，哪个是 UDP 的 特征A．拥塞控制B．可靠C．流量控制D．无需连接

10. 忘了

选择题都很简单。

五道大题。

## 二、拥塞控制（TCP Reno）

1. 写每个点发生的事，比如超时，收到三个冗余 ACK 之类。

2. 求一个点的拥塞窗口大小，以及窗口阈值大小。

3. 判断慢启动和拥塞避免，写它们的特征和作用。

## 三、LS 算法

Dijkstra+写算法收敛时 A 的路由表项。

图长下边这样：

<!-- image-->

## 四、1. 为什么 WiFi 不能用 CSMA/CD

2．WiFi 怎么避免冲突

五、 卫星通信带宽是 800Mbps，一条数据 1000B，卫星到地球距离 600km，采用流水线类rdt， 或停等协议

1. 选哪个协议链路利用率大

2. 求最大利用率，以及此时的窗口大小

3. 在 2 的前提下，问 sequence 用几位序号表示

## 六、NAT 技术

1. 一个服务器 IP 是 192.168.0.2，连着一个交换机，交换机连接一个路由器 R，左端 IP 是192.168.0.1 ，右端 IP 是 202.155.0.1，右端连接一个交换机，交换机连接一个主机 A，ip 地址为 202.155.0.2，现在主机 A 要访问服务器的 8080 端口，请问 R 的 NAT 表项怎么设置（数据有出入）

2.主机 A 的 ARP 表项为空，现在要给服务器发一个数据 P，问之后的行为，ARP 的数据类型，源 IP，源 MAC，目的 IP，目的 MAC，然后完成 ARP 表项，并回答 TTL 的作用。

3. 问发数据时，R 左右两端口的 IP，MAC

4. HTTP1.1pipeline 协议下，申请一个网页，网页有五个图片，（图片大小忽略不计）问总共需要几个 RTT。