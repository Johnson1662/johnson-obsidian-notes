## 一、选择题

1. 主机和主机之间建立了 TCP 连接，A 向 B 发送了一个报文段，其中 seq=199，ack=200，数据部分有两个字节，则主机 B 对该报文的确认报文段中（）。  
A. seq =201, ack=200  
B. seq =200, ack=201  
C. seq =200, ack=201  
D. seq =202, ack=201

2. 下列关于 TCP 和 UDP 的说法，错误的是（）。A. TCP 是面向连接的服务，在数据传输前要进行"三次握手"B. UDP 提供一种不可靠数据传送服务，是无连接的C. UDP 报文字段中的确认号，用于接收方向发送方确认报文接收D. TCP 拥塞控制主要包括慢启动、拥塞避免、快速恢复等技术

3. 考虑通过固定的路由从源主机发送数据包到目标主机，下面哪个延迟可能会发生变化（）。A. 节点处理延迟  
B. 传输延迟  
C. 传播延迟  
D. 排队延迟

4. 给定子网 20.1.24.0/24，则该子网能支持的可用用户数量和子网掩码是多少（）。  
A. 246；255.255.255.0  
B. 254；255.255.255.0  
C. 128；255.255.255.0  
D. 126；255.255.248.0

5. Persistent HTTP 与 Non-persistent HTTP 的区别在于（）A. Persistent HTTP 不需要建立传输层的 TCP 连接B. Persistent HTTP 在应用层与传输层之间采用 Socket 通信C. Persistent HTTP 可以通过一个 TCP 连接请求多个对象（Objects）D. Persistent HTTP 在两个 RTT 时间内只能传输一个对象（object）

6.关于 P2P (Peer-to-Peer) 和 C/S (Client/Server) 网络应用架构，下列说法正确的是（）。  
A. P2P 应用系统中的节点只能是客户端或者是服务器。  
B. P2P 应用架构需要在 TCP/IP 协议栈增加功能来进行支持。  
C. 在 P2P 架构下，每个 peer 既可以提供服务也可以请求服务。  
D. P2P 架构总比 C/S 架构性能优越。9.下列关于 CSMA/CA 协议的说法错误的是（）。  
A. 在数据帧发送过程中能够检测到信道冲突 (collision)。  
B. 在发送数据帧前要先侦听信道的状态。  
C. 监测到信道忙时，随机等待一段时间再继续监测信道。  
D. 使用停 - 等 (Stop-and-Wait) 可靠传输来应对信道冲突产生的丢包。

10.下列说法中错误的是（）。

A. IP 层可以屏蔽各个物理网络的差异。

B. IP 层可以代替各个物理网络和数据链路层工作。

C. IP 层可以隐藏各个物理网络的实现细节。

D. IP 层可以提供转发和路由的功能。

## 二、简答题

以给定网络拓扑为例，主机 A 向主机 B 发送数据，从网络层和链路层简要描述数据发送的过程，并且说明在此过程中数据包头部的源和目的MAC地址。

<!-- image-->

三、某校园网有两个局域网通过路由器 R_1、R_2 和 R_3 互联后接入 Internet,S_1 和 S_2 为以太网交换机。局域网采用静态 IP 地址配置，路由器部分接口以及各主机的 IP 地址如下图所示：

<!-- image-->

假设 NAT 转换表结构为
<table><tr><td rowspan=1 colspan=1>外网</td><td rowspan=1 colspan=1></td><td rowspan=1 colspan=1>内网</td><td rowspan=1 colspan=1></td></tr><tr><td rowspan=1 colspan=1>IP 地址</td><td rowspan=1 colspan=1>端口号</td><td rowspan=1 colspan=1>IP 地址</td><td rowspan=1 colspan=1>端口号</td></tr></table>

## 四、路由算法题

网络拓扑如下图所示：

(1) 请使用 DV (Distance Vector，距离向量) 方法计算从节点 X 到其它各节点的最短路径，将计算过程填写在下方表格中 (10 分)

<!-- image-->

<!-- image-->

(2) 请在下表中填上算法收敛时节点 X 的路由表项：

<table><tr><td rowspan=1 colspan=1>Destination</td><td rowspan=1 colspan=1>Nexthop</td><td rowspan=1 colspan=1>Cost</td></tr><tr><td rowspan=1 colspan=1></td><td rowspan=1 colspan=1></td><td rowspan=1 colspan=1></td></tr><tr><td rowspan=1 colspan=1></td><td rowspan=1 colspan=1></td><td rowspan=1 colspan=1></td></tr></table>

## 五、交换方式计算题

假设用户共享一条 100Mbps 的链路，又设每用户传输数据时需 1Mbps 的带宽，并且每个用户仅有 10%的时间用于传输数据，其它时间空闲，请回答下列问题：

(1) 如果使用电路交换（circuit switching），最多能支持多少个用户？

(2) 如果使用分组交换（packet switching），假设有 N 个用户，给出在任意时刻有多于 M 个用户同时传输的概率表达式。

(3) 分析说明Internet采用分组交换的原因。

六、实验在一条带宽为 R 的链路（图中 connection）上传输一个长度为 L 的数据分组。实验测得 t_0 时刻数据分组的第一个 bit 进入该链路，t_1 时刻数据分组的第一个 bit 离开该链路，t_2 时刻数据分组的最后一个 bit 离开该链路。各时刻示意如图所示，根据下述情况作答：(15 分)

(1) 使用上述实验中的变量符号表示该数据分组的传播时延、传输时延和吞吐量。(3 分)

(2) 写出端到端总延迟计算式子（式子中要包含分组长度 L）(3 分)

(3) 假定链路传播时延为 500ms，带宽 1Mb/s，计算要将一封分组长度为 4Kb 的 email 邮件传输出去所需要的端到端延迟 (6 分)

(4) 按照和 (3) 同样的带宽和传播延迟假设，需要把 32Gb 的数据从天津交付给北京市的朋友，时间紧急，你会选择以什么方式来交付？说出原因 (3 分)

<!-- image-->

## 七、TCP 拥塞控制题

主机 A 通过 TCP 连接发送一个文件到主机 B，TCP 协议使用 Tahoe 版本，下图画出了拥塞

窗口随时间变化的情况，其中发生的事件使用从1到6的序号进行标记，请回答如下问题：

(1) 当事件 1、2 和 3 发生时，主机 A 上的 TCP 正处于何种状态？

(2) 在事件4时刻发送方的窗口大小是多少？

(3) 简要解释事件 5 和 6 时刻 TCP 所处阶段，窗口变化特点以及这么设计的原因。

<!-- image-->  
八、简述 CSMA/CD 中指数退避的基本思想并解释该机制能够有效减少冲突的原因。