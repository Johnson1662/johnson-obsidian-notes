---
title: 首页
custom_instructions: |
  ## 会话启动协议 (Session Start Protocol)

  每次新对话开始时，**立即执行**以下步骤：

  1. **读取核心记忆** (Mandatory)
     - 使用 `Read` 工具读取 `核心配置/助手记忆.md`
     - 加载身份、身体状态、环境配置、2026目标

  2. **时间校验** (Mandatory)
     - 使用 `bash` 执行 `date` 命令获取实时时间
     - 所有时间相关查询必须基于实时结果，禁止依赖静态上下文

  3. **检查今日规划** (Optional)
     - 如果存在 `每日规划/YYYY-MM-DD.md`，读取并了解今日任务

  ## 记忆更新触发器 (Memory Update Triggers)

  当检测到以下变化时，**主动提议**更新 `核心配置/助手记忆.md`：

  - **身体数据变更**：体重、身高、健身水平、器材变化
  - **环境变更**：工具链更新、系统配置变更
  - **目标调整**：寒假目标、优先级变化
  - **作息变更**：睡眠时间调整

  更新时使用 `/update-memory` 技能或直接编辑文件。
---

# 🚀 AI Sophomore Digital Garden

> [!important] 核心记忆
> 本系统的灵魂存储在 [[助手记忆]] 中，已配置**自动加载**。

> [!tip] 记忆管理
>
> - 使用 `/start-day` 启动新的一天
> - 使用 `/update-memory` 更新个人档案或目标

## 📂 目录结构

- **`知识库`**：你的硬核大脑。并行计算、CSAPP、数理基础。
- **`每日规划`**：核心习惯。早起规划，晚睡复盘。
- **`项目`**：实战演练。MiniMind、大模型量化、寒假详细计划。
- **`核心配置`**：存储你的个人档案、健身数据和系统记忆。
- **`资源`**：参考资料。八股文、课程表。

## 🤖 助手指令 (Skills)

- **`/start-day`**：扫描“项目”进度与“每日规划”记录，生成今天的任务清单。
- **`/ai-research`**：辅助科研。针对量化或算法难题进行调研，并将成果直接存入你指定的“知识库”子目录。
- **`/review`**：复盘计划执行，根据你的笔记更新进度，调整任务优先级。
- **`/check`**：检查你的知识库结构，帮你发现笔记间的潜在联系（Wikilinks）。

---

_Everything orbits around your learning and planning._
