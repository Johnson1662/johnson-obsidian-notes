Boring count

  

Contest Problem

Time Limit：C/C++ 1000MS，Other 2000MS  
Memory Limit：C/C++ 32MB，Other 64MB  
Level： Beginner  
Created By：  

Description

You are given a string S consisting of lowercase letters, and your task is counting the number of substring that the number of each lowercase letter in the substring is no more than K.

Input

In the first line there is an integer T , indicates the number of test cases.  
For each case, the first line contains a string which only consist of lowercase letters. The second line contains an integer K.  
  
[Technical Specification]  
1<=T<= 100  
1 <= the length of S <= 100000  
1 <= K <= 100000

Output

For each case, output a line contains the answer.

Sample Input 1 

3
abc
1
abcabc
1
abcabc
2

Sample Output 1 

6
15
21

```C++
#include <iostream>
#include <string>
#include <vector>

using namespace std;

typedef long long ll;

void solve() {
    string s;
    int k;
    if (!(cin >> s >> k)) return;

    int n = s.length();
    vector<int> count(26, 0); // 记录窗口内每个字母出现的次数
    ll totalSubstrings = 0;
    int left = 0;

    for (int right = 0; right < n; ++right) {
        int charIdx = s[right] - 'a';
        count[charIdx]++;

        // 如果当前字母超过了限制 K，缩小左边界
        while (count[charIdx] > k) {
            count[s[left] - 'a']--;
            left++;
        }

        // 窗口 [left, right] 及其内部所有以 right 结尾的子串都符合要求
        totalSubstrings += (right - left + 1);
    }

    cout << totalSubstrings << endl;
}

int main() {
    // 优化 IO
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    if (!(cin >> t)) return 0;
    while (t--) {
        solve();
    }
    return 0;
}
```