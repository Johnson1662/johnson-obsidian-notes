```mermaid
flowchart TD
    %% 外部流量源
    Traffic["网络流量源\n(校园/工业网 + 模拟攻击\nhping3/Scapy等)"]

    %% 1. 数据平面
    subgraph 数据平面 ["1. 数据平面 (P4 可编程数据平面)"]
        direction TB
        P4Switch["P4 Programmable Switch\n(BMv2 / Tofino / SONiC)"]
        INT["In-band Network Telemetry\n(INT v2.1 物理特性采集)"]
        P4Switch -->|实时解析五元组 + 初步过滤| INT
    end

    %% 2. 采集层
    subgraph 采集层 ["2. 采集层"]
        direction TB
        Collector["Collector Agent\n(流特征提取 + INT物理数据解析)"]
    end

    %% 3. 控制平面 - 多Agent系统
    subgraph 控制平面 ["3. 控制平面 (多智能体系统 CrewAI / LangGraph)"]
        direction TB
        Detector["Detector Agent\n(轻量初筛 + 异常判断)"]
        Classifier["Classifier Agent\n(SecGPT 精细多类分类)"]
        Explainer["Explainer Agent\n(SecGPT + RAG\n物理解释 + 攻击链报告)"]
        Defender["Defender Agent\n(防御决策 + P4Runtime指令)"]
        Supervisor["Supervisor Agent\n(协调 + 争议解决 + 日志)"]

        Detector --> Classifier
        Classifier --> Explainer
        Explainer --> Defender
        Supervisor -.-> Detector
        Supervisor -.-> Classifier
        Supervisor -.-> Explainer
    end

    %% 4. 知识模型层
    subgraph 知识模型层 ["4. 知识模型层"]
        direction TB
        SecGPT["SecGPT\n(网络安全专用大模型\n7B/14B vLLM部署)"]
        RAG["RAG知识库\n(MITRE ATT&CK + INT模板\n历史案例 + 学校网络拓扑)"]
    end

    %% 5. 执行与展示层
    subgraph 执行与展示层 ["5. 执行与展示层"]
        direction TB
        ONOS["P4Runtime / ONOS控制器\n(动态下发流表)"]
        Dashboard["Web可视化仪表盘\n(Echarts + Grafana)\n(实时热力图 + Agent对话\n分类报告 + PDF导出)"]
    end

    %% 主要数据流向
    Traffic --> P4Switch
    INT --> Collector
    Collector --> Detector
    Classifier <--> SecGPT
    Explainer <--> SecGPT
    Explainer <--> RAG
    Defender --> ONOS
    ONOS --> P4Switch
    Explainer --> Dashboard
    Defender --> Dashboard
    Dashboard -.->|实时反馈与监控| Traffic
```
